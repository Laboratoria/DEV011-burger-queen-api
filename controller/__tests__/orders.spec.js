const {
    getOrders,
  } = require('../orders');
  
  const Order = require('../../models/orders');
  
  describe('getOrders', () => {
    it('should get orders collection', (done) => {
      done();
    });
  });
  